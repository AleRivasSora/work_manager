import { ValidatePaginationPipe } from './validate-pagination.pipe';

describe('ValidatePaginationPipe', () => {
  it('should be defined', () => {
    expect(new ValidatePaginationPipe()).toBeDefined();
  });
});
